in: query
name: 'filter[link]'
required: true
schema:
  type: string
  description: >-
    Filter by parent links. If link param is not specified, permissions for highest level 
    are returned, if specified permissions where this link is parent are returned.
  example: "tokend/"